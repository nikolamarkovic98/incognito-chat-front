import { HoldDirective } from './hold.directive';

describe('HoldDirective', () => {
  it('should create an instance', () => {
    const directive = new HoldDirective();
    expect(directive).toBeTruthy();
  });
});
